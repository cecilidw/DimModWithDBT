version: 1

models:
  - name: stg_actor
    columns:
      - name: actor_id
        type: int
        tests:
          - unique
      - name: first_name
        type: string
      - name: last_name
        type: string
      - name: last_update
        type: timestamp